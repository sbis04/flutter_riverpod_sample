name: flutter_riverpod_sample
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
    sdk: ">=2.7.0 <3.0.0"

dependencies:
    flutter:
        sdk: flutter

    cupertino_icons: ^1.0.3
    flutter_riverpod: ^0.14.0+3
    json_annotation: ^4.0.1
    dio: ^4.0.0

dev_dependencies:
    flutter_test:
        sdk: flutter
    freezed: ^0.14.1+3
    build_runner: ^2.0.2
    json_serializable: ^4.1.1

flutter:
    uses-material-design: true
